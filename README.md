1. make copy of .env.example or .env.copy file and re-name it as .env
2. open terminal and run 'composer install' and then 'npm install'
3. you can check project by runing 'php aritsan serve' 
4. add all the files and folders into server
5. once after uploading all the files and folders, re-name '.htaccess_' file as '.htaccess'
6. then run the url.